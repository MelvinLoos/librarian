# Exclude your local library folders (update this name if your folder is called something else!)
library/
Calibre_Library/
*.db

# Git
.git
.gitignore

# Node modules (Docker should install its own inside the container)
node_modules/
frontend/node_modules/
backend/node_modules/

# Build outputs (Docker should build these itself)
.output/
.nuxt/
backend/dist/
frontend/.nuxt/

# Environment files
.env